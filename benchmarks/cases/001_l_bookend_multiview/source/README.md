# Supplied drawing

`drawing.png` is preserved byte-for-byte from the image supplied by the user.

The drawing contains a front view on the left and an L-shaped right view on the right. These labels are supported by the reference SCAD header; the projection standard is intentionally left unknown.
