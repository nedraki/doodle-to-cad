# Supplied drawing

`drawing.png` is preserved byte-for-byte from the user-supplied image. It shows a large face view with two rounded openings and seven narrow slots, plus a thin projected view above it.

The vertical divisions in the upper view may be correspondence marks rather than physical seams. They are not represented as features in the supplied reference CAD and must not be scored as required geometry.
